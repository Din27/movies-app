<h1>View Actor: <%= @actor.name %></h1>

<p>
  <strong>Date of Birth:</strong> <%= @actor.dob %>
</p>

<p>
  <strong>Movies:</strong>
  <ul>
    <% @actor.movies.each do |movie| %>
      <li><%= link_to movie.title, movie_path(movie) %> (<%= movie.year %>)</li>
    <% end %>
</ul>
</p>


<%= link_to 'Edit', edit_actor_path(@actor) %><br/>
<%= link_to 'Delete', actor_path(@actor),
            method: :delete,
            data: { confirm: "Are you sure you wish to delete the actor '#{@actor.name}?'" } %><br/>
<%= link_to 'Back', actors_path %>